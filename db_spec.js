var jasmine = require('jasmine-node');

describe('DB', function() {
  // it('should attempt to connect to redis', function () {
  //   var DB = require('../lib/db');
  //   expect(redis).to.
  // });
});
